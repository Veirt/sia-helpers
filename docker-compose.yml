services:
  sia-helpers:
    container_name: "sia-helpers"
    image: ghcr.io/veirt/sia-helpers:latest
    build: .
    volumes:
      - ./data:/app/data
    env_file: .env
    ports:
      - 33125:33125
      - 33126:33126
    restart: unless-stopped
