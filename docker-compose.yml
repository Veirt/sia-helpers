version: "3"
services:
  app:
    build: .
    container_name: "sia-helper"
    environment:
      - NIM=
      - PASSWORD=
    volumes:
      - /path/to/data:/app/data
    ports:
      - 8080:8080
    restart: unless-stopped
